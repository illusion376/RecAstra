# Диапазоны, а не жёсткие пины: на Python 3.14 старые сборки pydantic-core
# не встают (нет колёс, тянется компиляция Rust). Проверено на 3.11 и 3.14.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
httpx>=0.27,<1.0
# Нужен для multipart-загрузки записи в POST /meetings
python-multipart>=0.0.9

# Распознавание речи через Nexara. Нужен, если STT_PROVIDER=nexara;
# без него бэкенд работает с готовой расшифровкой.
nexara>=0.5,<1.0